app:
  name: svc
  image:
    repository: "onecx/onecx-human-task-svc"
  db:
    enabled: true
  operator:
    keycloak:
      client:
        enabled: true
        spec:
          kcConfig:
            defaultClientScopes: [ ocx-ht:read ]
    microservice:
      spec:
        description: "OneCX backend service"
        name: "onecx-human-task-svc"